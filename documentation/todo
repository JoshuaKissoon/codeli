Codeli Todo
- Handle system installation


Later On
- 
- Allow a module to specify CSS & Script files that needs to be loaded
- Allow a Route to have a specific file in which it's callback is stored
- Allow a module to be dependent on another module
- Ensure that routes are deleted for non-existent modules
- Ensure that permissions are deleted for non-existent modules